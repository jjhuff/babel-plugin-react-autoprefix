{
  "name": "babel-plugin-react-autoprefix",
  "version": "0.2.6",
  "description": "Adds vendor prefixes to inline styles in React elements",
  "repository": "UXtemple/babel-plugin-react-prefix-styles",
  "license": "MIT",
  "main": "index.js",
  "devDependencies": {
    "babel-core": "^6.5.0",
    "babel-cli": "^6.5.0",
    "babel-preset-stage-0": "^6.5.0",
    "babel-preset-react": "^6.5.0",
    "tape": "^4.0.1"
  },
  "scripts": {
    "build": ".bin/build",
    "clean": ".bin/clean",
    "push": ".bin/push",
    "test": ".bin/test"
  },
  "keywords": [
    "babel-plugin",
    "react",
    "auto",
    "prefix",
    "vendor",
    "style",
    "inline",
    "styles"
  ],
  "dependencies": {
    "autoprefix": "^0.1.2"
  }
}
